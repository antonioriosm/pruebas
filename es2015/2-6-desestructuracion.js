'use strict';
/**
 * desestructuración
 */
let { nombre, peso } = { nombre: 'manzana', peso: '250 gramos' };

console.log(nombre, peso);

let [a, b, c] = ['manzana', 'pera', 'uva'];

console.log(a, b, c);